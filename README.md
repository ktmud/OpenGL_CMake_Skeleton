# 3D Scanning with RealSense

Using Intel RealSense camera to scan a human body (or something else).

## Build Steps

```bash
git clone https://github.com/ktmud/realsense-scanbody.git --recursive
cd realsense-scanbody
```
